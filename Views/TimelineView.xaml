<UserControl x:Class="JournalApp.Views.TimelineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:JournalApp.ViewModels"
             xmlns:converters="clr-namespace:JournalApp.Converters"
             d:DataContext="{d:DesignInstance Type=viewmodels:TimelineViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:XamlToPlainTextConverter x:Key="XamlToPlainTextConverter"/>
    </UserControl.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <DockPanel LastChildFill="False" Margin="0,0,0,16">
                <TextBlock Text="My Timeline" Style="{DynamicResource MaterialDesignHeadline4TextBlock}" DockPanel.Dock="Left"/>
                
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Search entries..." Margin="0,0,8,0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <Button Command="{Binding SearchCommand}" Style="{StaticResource MaterialDesignIconButton}">
                        <materialDesign:PackIcon Kind="Search"/>
                    </Button>
                </StackPanel>
            </DockPanel>

            <materialDesign:Card Padding="8" UniformCornerRadius="8">
                <StackPanel Orientation="Horizontal">
                    <DatePicker SelectedDate="{Binding StartDate}" materialDesign:HintAssist.Hint="Start Date" Width="120" Margin="0,0,8,0" Style="{DynamicResource MaterialDesignFloatingHintDatePicker}"/>
                    <DatePicker SelectedDate="{Binding EndDate}" materialDesign:HintAssist.Hint="End Date" Width="120" Margin="0,0,8,0" Style="{DynamicResource MaterialDesignFloatingHintDatePicker}"/>
                    <ComboBox ItemsSource="{Binding AllMoods}" SelectedItem="{Binding SelectedMood}" materialDesign:HintAssist.Hint="Filter Mood" Width="150" Margin="0,0,8,0" Style="{DynamicResource MaterialDesignFloatingHintComboBox}"/>
                    <Button Content="APPLY FILTERS" Command="{Binding SearchCommand}" Style="{DynamicResource MaterialDesignFlatButton}" VerticalAlignment="Bottom"/>
                    <Button Content="RESET" Command="{Binding LoadEntriesCommand}" Style="{DynamicResource MaterialDesignFlatButton}" Foreground="#808080" VerticalAlignment="Bottom"/>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding Entries}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Margin="0,0,0,12" Padding="16" UniformCornerRadius="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
    
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0">
                                        <TextBlock Text="{Binding Date, StringFormat=MMM}" Style="{StaticResource MaterialDesignCaptionTextBlock}" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding Date, StringFormat=dd}" Style="{StaticResource MaterialDesignHeadline5TextBlock}" HorizontalAlignment="Center"/>
                                    </StackPanel>
    
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Title}" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" FontWeight="Bold"/>
                                        
                                        <!-- Content snippet - hidden by default unless pinned -->
                                        <TextBlock Text="{Binding Content, Converter={StaticResource XamlToPlainTextConverter}}" 
                                                   MaxHeight="100" 
                                                   TextWrapping="Wrap"
                                                   TextTrimming="CharacterEllipsis" 
                                                   Opacity="0.7" 
                                                   Margin="0,4,0,0"
                                                   Visibility="{Binding ElementName=ExpandToggle, Path=IsChecked, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        
                                        <ItemsControl ItemsSource="{Binding Tags}" Margin="0,4,0,0">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <materialDesign:Chip Content="{Binding Name}" Margin="0,0,4,0" IsEnabled="False" Height="24"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
    
                                    <StackPanel Grid.Column="2" VerticalAlignment="Top">
                                        <TextBlock Text="{Binding PrimaryMood}" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryHueMidBrush}" HorizontalAlignment="Right"/>
                                        <ToggleButton x:Name="ExpandToggle" 
                                                      Style="{StaticResource MaterialDesignActionLightToggleButton}" 
                                                      ToolTip="Pin to see details" 
                                                      Width="30" Height="30" Margin="0,8,0,0">
                                            <ToggleButton.Content>
                                                <materialDesign:PackIcon Kind="PinOutline" Width="18" Height="18" Opacity="0.5"/>
                                            </ToggleButton.Content>
                                            <materialDesign:ToggleButtonAssist.OnContent>
                                                <materialDesign:PackIcon Kind="Pin" Width="18" Height="18"/>
                                            </materialDesign:ToggleButtonAssist.OnContent>
                                        </ToggleButton>
                                    </StackPanel>
                                </Grid>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <Button Content="LOAD MORE" 
                        Command="{Binding LoadMoreCommand}" 
                        Visibility="{Binding CanLoadMore, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Style="{DynamicResource MaterialDesignOutlinedButton}"
                        Margin="16,16,16,32"
                        HorizontalAlignment="Center"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
