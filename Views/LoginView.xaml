<UserControl x:Class="JournalApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:JournalApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:LoginViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <materialDesign:Card Width="300" Padding="32" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Journal Login" Style="{DynamicResource MaterialDesignHeadline5TextBlock}" HorizontalAlignment="Center" Margin="0,0,0,24"/>
                
                <PasswordBox x:Name="PinBox" 
                             materialDesign:HintAssist.Hint="Password / PIN" 
                             Style="{DynamicResource MaterialDesignFloatingHintPasswordBox}" 
                             PasswordChanged="PinBox_PasswordChanged"
                             Margin="0,0,0,16"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="0,0,0,16" />

                <Button Content="LOGIN" Command="{Binding LoginCommand}" Margin="0,8,0,0" />
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
