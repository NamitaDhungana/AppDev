<UserControl x:Class="JournalApp.Views.EntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:JournalApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:EntryViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,16">
            <StackPanel Orientation="Vertical" DockPanel.Dock="Left">
                <TextBlock Text="{Binding Date, StringFormat=D}" Style="{DynamicResource MaterialDesignHeadline5TextBlock}" />
                <TextBlock Text="Daily Journal" Style="{DynamicResource MaterialDesignSubtitle1TextBlock}" />
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Command="{Binding CreateNewCommand}" Style="{DynamicResource MaterialDesignRaisedButton}" Content="NEW" Margin="0,0,8,0" Background="#4CAF50" BorderBrush="#4CAF50" />
                
                <!-- SAVE button for new entries -->
                <!-- Single Save/Update Button -->
                <Button Command="{Binding SaveOrUpdateCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Content="SAVE / UPDATE" Margin="0,0,8,0" />

                <Button Command="{Binding DeleteCommand}" Style="{DynamicResource MaterialDesignOutlinedButton}" Content="DELETE" Foreground="Red" BorderBrush="Red" />
            </StackPanel>
        </DockPanel>

        <!-- Metadata Inputs -->
        <materialDesign:Card Grid.Row="1" Padding="16" UniformCornerRadius="8" Margin="0,0,0,16" materialDesign:ElevationAssist.Elevation="Dp2">
            <StackPanel>
                <!-- Row 1: Title -->
                <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" 
                         materialDesign:HintAssist.Hint="Title of your day..." 
                         Style="{DynamicResource MaterialDesignOutlinedTextBox}" 
                         Margin="0,0,0,16" 
                         FontSize="18" 
                         FontWeight="SemiBold"/>

                <!-- Row 2: Moods -->
                <TextBlock Text="How are you feeling?" Style="{DynamicResource MaterialDesignCaptionTextBlock}" Margin="0,0,0,8" Opacity="0.6"/>
                <UniformGrid Columns="3" Margin="0,0,0,16">
                    <ComboBox ItemsSource="{Binding PositiveMoods}" 
                              Text="{Binding PrimaryMood}"
                              IsEditable="True"
                              materialDesign:HintAssist.Hint="Primary Mood (Type/Select)" 
                              Style="{DynamicResource MaterialDesignOutlinedComboBox}" 
                              Margin="0,0,8,0"/>
                    <ComboBox ItemsSource="{Binding NeutralMoods}" 
                              SelectedItem="{Binding SecondaryMood1}" 
                              materialDesign:HintAssist.Hint="Secondary (Neutral)" 
                              Style="{DynamicResource MaterialDesignOutlinedComboBox}" 
                              Margin="0,0,8,0"/>
                    <ComboBox ItemsSource="{Binding NegativeMoods}" 
                              SelectedItem="{Binding SecondaryMood2}" 
                              materialDesign:HintAssist.Hint="Optional (Negative)" 
                              Style="{DynamicResource MaterialDesignOutlinedComboBox}"/>
                </UniformGrid>

                <!-- Row 3: Tags -->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBox Grid.Row="0" Margin="0,0,0,8"
                             Text="{Binding TagsInput, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Your Tags"
                             materialDesign:TextFieldAssist.HasClearButton="True"
                             Style="{DynamicResource MaterialDesignOutlinedTextBox}" />

                    <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding QuickTags}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding}" 
                                            Command="{Binding DataContext.AddQuickTagCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{DynamicResource MaterialDesignFlatButton}"
                                            ContentStringFormat="#{0}"
                                            Height="28"
                                            Padding="12,0"
                                            Margin="0,0,8,0"
                                            FontSize="12"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </StackPanel>
        </materialDesign:Card>

        <!-- Editor -->
        <materialDesign:Card Grid.Row="2" Padding="16" UniformCornerRadius="8">
            <TextBox Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True" 
                     TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto"
                     Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                     BorderThickness="0.5"
                     materialDesign:HintAssist.Hint="Write your thoughts here..."
                     VerticalContentAlignment="Top"
                     FontSize="16"
                     MinHeight="400"
                     Height="Auto" />
        </materialDesign:Card>
        
        <!-- Status Bar or Footer -->
        <ProgressBar Grid.Row="3" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,16,0,0"/>
    </Grid>
</UserControl>
